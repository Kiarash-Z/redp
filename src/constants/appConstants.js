const APP_NAME = 'Music Player';
const SIZE = { width: 1000, height: 750 };
const MAX_SIZE = { width: 1200, height: 900 };
const MIN_SIZE = { width: 420, height: 530 };

module.exports = { APP_NAME, SIZE, MAX_SIZE, MIN_SIZE };
