import React, { Component } from 'react';

import './Nav.css';

class Nav extends Component {
  render() {
    return (
      <nav className="nav">
        <i className="a-left"/>
        <i className="a-list"/>
      </nav>
    );
  }
}

export default Nav;